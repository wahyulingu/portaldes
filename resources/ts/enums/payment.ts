export enum Payment {
    required = 'rquired',
    paid = 'paid',
    accepted = 'accepted',
    late = 'late',
    canceled = 'canceled',
    refunding = 'refunding',
    refunded = 'refunded',
    rejected = 'rejected'
}
